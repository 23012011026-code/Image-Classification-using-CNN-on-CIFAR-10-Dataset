🚀 CIFAR-10 Image Classification using CNNThis project was developed as a Capstone Project to demonstrate the power of Deep Learning in computer vision. Using TensorFlow and Keras, I built a Convolutional Neural Network (CNN) capable of identifying objects across 10 different classes (e.g., airplanes, automobiles, birds, cats).

🧠 Model ArchitectureThe model uses a sequential approach to extract features and classify images:Convolutional Layers: Extracts spatial features using filters.Pooling Layers (MaxPooling): Reduces dimensionality and computational load.Dropout Layers: Prevents overfitting by randomly deactivating neurons during training.Dense Layers: Fully connected layers for final classification using the Softmax activation function.🛠️ Tech StackLanguage: PythonFrameworks: TensorFlow, KerasLibraries: NumPy (Data manipulation), Matplotlib (Visualization), Scikit-learn (Evaluation)📊 Dataset Overview.

The CIFAR-10 dataset consists of 60,000 32x32 color images in 10 classes, with 6,000 images per class. There are 50,000 training images and 10,000 test images.ClassExamplesTransportationAirplane, Automobile, Ship, TruckAnimalsBird, Cat, Deer, Dog, Frog, Horse📈 Results & InsightsData Augmentation: Applied techniques like horizontal flipping and rotation to increase model robustness.Normalization: Scaled pixel values to the range [0, 1] for faster convergence.Evaluation: (Insert your final test accuracy here, e.g., Achieved 82% accuracy on the test set).
